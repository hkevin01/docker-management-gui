# Docker Management GUI

## Environment Variables

# API Base URL (web uses relative /api in production behind nginx). Override for local dev if needed.
VITE_API_URL=/api

# Docker Settings
DOCKER_SOCKET_PATH=/var/run/docker.sock
SAFE_MODE=false
RATE_LIMIT_MAX=200
RATE_LIMIT_WINDOW=1 minute
ALLOWED_ORIGINS=

# Logging
LOG_LEVEL=info

# Server
PORT=3001
HOST=localhost

# E2E
E2E_BASE_URL=http://localhost:8080
